/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['https://crlgtcirebmfbugrnchs.supabase.co'], // We'll update this later with your actual Supabase URL
  },
}

module.exports = nextConfig